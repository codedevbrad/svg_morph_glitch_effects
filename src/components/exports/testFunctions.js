var oldx = 0,
    oldy = 0,
    coordinates = [ ];

export const movement = ( ( e ) => ({

         name : 0,
         getCurrentVal ( ) {
            this.name = this.name + 1;
 				    console.log( 'hi' , this.name );
         }

}))();
